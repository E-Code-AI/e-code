export default function Counter() {
  // Implement the counter using React.useState<number> with handlers for increment, decrement, and reset. Render the current count prominently and provide three buttons with onClick handlers, keyboard accessibility, and basic ARIA labeling if appropriate.
  return (
    <div>
      <h1>Counter</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}