export default function Counter() {
  // Implement a functional Counter component using React useState with proper TypeScript typing for the count state and handlers. Provide increment, decrement, and reset buttons, display the current count, and optionally accept initial count via props with default values.
  return (
    <div>
      <h1>Counter</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}