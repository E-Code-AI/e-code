/**
 * Basic Lighthouse performance check configuration
 * To be expanded with actual Lighthouse programmatic runs.
 */
console.log('Lighthouse performance tests loaded.');
// Require a minimum score for production:
const MIN_SCORE = 90;
console.log(`Performance target: ${MIN_SCORE}/100`);
