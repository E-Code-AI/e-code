export default function Layout() {
  // Provide a simple layout wrapper that centers the todo app and adds consistent padding, max-width, and background styling. Accept children and render them within a styled container.
  return (
    <div>
      <h1>Layout</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}