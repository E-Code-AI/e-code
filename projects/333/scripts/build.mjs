ES module Node script that configures and runs esbuild to bundle the React + TypeScript app. Define common options (entry point, outdir, JSX, sourcemaps) and support different modes (development watch vs production minified build) based on CLI arguments or environment variables.

TODO: Implement file content based on outline
