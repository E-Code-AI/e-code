export default function TodoFilters() {
  // Provide UI controls for selecting the current filter (all, active, completed) and optionally a button to clear completed todos. Accept current filter, counts, and callback props to change filter and clear completed.
  return (
    <div>
      <h1>TodoFilters</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}