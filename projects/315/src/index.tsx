export default function index() {
  // Create the entry point for the React application. This file will import the main App component and render it into the root DOM element defined in public/index.html.
  return (
    <div>
      <h1>index</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}