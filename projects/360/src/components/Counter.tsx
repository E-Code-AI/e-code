export default function Counter() {
  // Create a React functional component that uses the useState hook to manage a numeric counter. Add two buttons to increment and decrement the count, ensuring the displayed value updates correctly on each click.
  return (
    <div>
      <h1>Counter</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}