export default function TodoInput() {
  // Render an input field and add button (or handle Enter key) to create new todos. Accept a callback prop for adding a todo and manage local input state with validation (e.g., ignore empty strings).
  return (
    <div>
      <h1>TodoInput</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}