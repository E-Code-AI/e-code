export default function TodoList() {
  // A React functional component that receives an array of todo items and renders each one using the TodoItem component. It also passes down the delete handler to each item.
  return (
    <div>
      <h1>TodoList</h1>
      <p>TODO: Implement component based on outline</p>
    </div>
  );
}