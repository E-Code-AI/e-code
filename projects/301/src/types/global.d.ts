/**
 * Global TypeScript declarations for any custom types not tied to a specific module, such as worker message types or window extensions. Ensures TypeScript recognizes Vite-specific imports if needed.
 */

// TODO: Implement based on outline
export {};
